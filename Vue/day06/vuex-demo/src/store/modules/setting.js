// setting模块
const state = {
  theme: 'light',
  fontSize: 16
}
const mutations = {}
const actions = {}
const getters = {}
export default {
  // 1.开启命名空间
  namespaced: true,
  state,
  mutations,
  actions,
  getters
}
